#include <stdio.h>

int main()
{
    int *ptr = NULL; // Good practise of assign NULL in pointer value at the time of declaration
    printf("The value of the null pointer is: %d\n", ptr);

    return 0;
}
